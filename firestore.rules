service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=diary} {
      allow read, write;
    }
    match /{document=**} {
      allow read: if request.auth != null;
      allow write;
    }
  }
}