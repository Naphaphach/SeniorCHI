service firebase.storage {
  match /b/{bucket}/o {
    match /profile {
      allow read: if request.auth != null;
      allow write;
    }

    match /{allPaths=**} {
      allow read, write;
    }
  }
}
